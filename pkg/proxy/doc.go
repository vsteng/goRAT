package proxy

//   proxyMgr.CloseProxy(proxyID)//   proxyID, err := proxyMgr.CreateProxy(clientID, remoteHost, remotePort)//   proxyMgr := proxy.NewManager(registry)// Usage:////   tunnel.go      - Tunnel implementation//   pool.go        - Connection pool//   manager.go     - Proxy tunnel manager// Structure://// This handles remote proxy creation, lifecycle, and pooled connections.// Package proxy contains proxy tunnel management and connection pooling.package proxy
